meta:
  environment: python
  toolchain:
    name: pip
    version: "3.11"
build:
  requirementsPath: requirements.txt
  useCache: true
run:
  scriptName: null
  command:  python manage.py runserver
  persistenceMount: /data
  containerPort: "80"
